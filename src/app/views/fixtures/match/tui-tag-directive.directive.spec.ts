import { TuiTagDirectiveDirective } from './tui-tag-directive.directive';

describe('TuiTagDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TuiTagDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
